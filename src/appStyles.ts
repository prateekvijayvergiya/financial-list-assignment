import styled from 'styled-components'

const TableStyled = styled.table`
  width: 100%;
  border-collapse: collapse;
  padding: 50px;
`
const TableCaption = styled.caption`
  font-weight: 700;
  font-size: larger;
`
export { TableStyled, TableCaption }
